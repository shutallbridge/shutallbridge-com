---
import { type HTMLAttributes } from "astro/types";

export interface Props extends HTMLAttributes<"span"> {}

const { ...rest } = Astro.props;
---

<span class="text-xl text-gray-500" slot="icon" {...rest}>
  <slot />
</span>
