---
import { type HTMLAttributes } from "astro/types";

export interface Props extends HTMLAttributes<"div"> {}

const { class: className, ...rest } = Astro.props;
---

<div class:list={["text-gray-600 md:max-w-32 md:w-full", className]} {...rest}>
  <slot />
</div>
